using UnityEngine;

public class Ejercicio15 : MonoBehaviour
{
    string[] inventario = {"Espada", "Poción", "Armadura", "Llave"};
    int m = 0;
    void Start()
    {
        while (m < inventario.Length)
        {
            Debug.Log("Posición " + m + ": " + inventario[m]);
            m++;
        }
    }
    void Update()
    {
        
    }
}
